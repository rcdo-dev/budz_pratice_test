name: budz_pratice_test
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  # Dependency injection and navigation
  flutter_modular: ^6.3.2

  # Design pattern
  bloc: ^8.1.2

  # State management
  flutter_bloc: ^8.1.3

  # Theme fonts
  google_fonts: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.3

flutter:
  uses-material-design: true
  assets:
    - assets/data/json_data.json
